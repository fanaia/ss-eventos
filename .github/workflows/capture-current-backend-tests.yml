name: capture-current-backend-tests

on:
  push:
    branches:
      - feat/itens-projeto-abas
    paths:
      - .github/workflows/capture-current-backend-tests.yml

permissions:
  contents: write

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/itens-projeto-abas
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Instalar backend
        working-directory: backend
        run: npm ci
      - name: Capturar testes
        run: |
          cd backend
          npm test > ../backend-test.log 2>&1 || true
      - name: Publicar log
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend-test.log
          git commit -m "ci: registrar saída atual dos testes backend"
          git push origin HEAD:feat/itens-projeto-abas
